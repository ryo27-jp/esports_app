<% if @teams.blank? %>
  <p>何もない</p>
<% else %>
  <% @teams.each do |team| %>
    <ul>
      <li>
        <%= link_to team.name, team_path(team.id), class:"" %>
      </li>
    </ul>
  <% end %>
<% end %>
